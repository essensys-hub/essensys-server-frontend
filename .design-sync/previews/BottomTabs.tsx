import { BottomTabs } from 'essensys-web-react';

export const Default = () => (
  <div className="relative h-40 w-full max-w-md border border-gray-200 bg-gray-50">
    <BottomTabs onMoreClick={() => {}} />
  </div>
);
